[project]
name = "reading"
version = "0.1.0"
description = "A personal Goodreads — static site generated from per-book Markdown files."
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
    "markdown>=3.5",
]

[tool.uv]
package = false
